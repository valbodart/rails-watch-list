<div class="container">
  <h1>Add a new list</h1>
<div class="simple-form">
    <%= simple_form_for [@list] do |f| %>
      <%= f.input :name %>
      <%= f.submit :submit %>
    <% end %>
</div>
<p><%= link_to 'My lists', lists_path %></p>
</div>
